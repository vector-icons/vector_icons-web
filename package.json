{
    "scripts": {
        "build": "npm run build:assets && npm run build:server && npm run build:client",
        "build:assets": "git submodule update --init --recursive",
        "build:client": "cd ./client && npm install && npm run build && cd ../",
        "build:server": "cd ./server && npm install && npm run build && cd ../"
    }
}